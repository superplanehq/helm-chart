apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-workers
  namespace: {{ .Release.Namespace }}
  labels:
    app: superplane
    service: {{ .Release.Name }}-workers
spec:
  replicas: {{ .Values.workers.replicas }}
  selector:
    matchLabels:
      app: superplane
      service: {{ .Release.Name }}-workers
  template:
    metadata:
      name: {{ .Release.Name }}-workers
      labels:
        app: superplane
        service: {{ .Release.Name }}-workers
    spec:
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Release.Name }}-workers
          image: "{{ .Values.image.registry }}/{{ .Values.image.name }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ .Values.domain.configMapName }}
            - secretRef:
                name: {{ include "secrets.database.name" . }}
            - secretRef:
                name: {{ include "secrets.rabbitmq.name" . }}
            - secretRef:
                name: {{ include "secrets.authentication.name" . }}
            - secretRef:
                name: {{ include "secrets.encryption.name" . }}
            - secretRef:
                name: {{ include "secrets.jwt.name" . }}
          env:
            - name: START_PENDING_EVENTS_WORKER
              value: "yes"
            - name: START_PENDING_STAGE_EVENTS_WORKER
              value: "yes"
            - name: START_TIME_WINDOW_WORKER
              value: "yes"
            - name: START_CONSUMERS
              value: "yes"
            - name: START_EXECUTIONS_POLLER
              value: "yes"
            - name: START_EXECUTION_RESOURCE_POLLER
              value: "yes"
            - name: START_PENDING_FIELD_SETS_WORKER
              value: "yes"
            - name: START_PENDING_EVENT_SOURCES_WORKER
              value: "yes"
            - name: START_PENDING_EXECUTIONS_WORKER
              value: "yes"
            - name: START_HARD_DELETION_WORKER
              value: "yes"
            - name: START_EVENT_SOURCE_SCHEDULE_WORKER
              value: "yes"
            - name: START_EVENT_DELETION_WORKER
              value: "yes"
            - name: RBAC_MODEL_PATH
              value: /app/rbac/rbac_model.conf
            - name: RBAC_ORG_POLICY_PATH
              value: /app/rbac/rbac_org_policy.csv
            - name: RBAC_CANVAS_POLICY_PATH
              value: /app/rbac/rbac_canvas_policy.csv
            - name: APPLICATION_NAME
              value: superplane
            - name: DB_POOL_SIZE
              value: "{{ .Values.workers.dbPoolSize }}"

          securityContext:
            privileged: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]

          {{- with .Values.workers.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
