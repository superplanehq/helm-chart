{{- if eq .Values.email.secretName "" }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "secrets.email.name" . }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  SENDGRID_API_KEY: {{ .Values.email.sendgridApiKey | b64enc }}
{{- end }}